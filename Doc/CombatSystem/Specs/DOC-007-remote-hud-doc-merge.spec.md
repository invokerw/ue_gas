# DOC-007 远端 HUD 与文档体系合并

> Spec 版本：`1.0`
> 状态：`READY_FOR_REVIEW`
> Owner：项目维护者
> 创建日期：2026-09-10
> 风险等级：`L1`
> 关联台账：[开发进度台账](../00-Project/00-01-Progress-Tracker.md)

## 1. 目标与范围

按用户要求拉取远端代码并合并文档修改。保留本地文档分层、目录编号、项目内 Skill、校验工具和已提交配置，将远端底部 HUD 的代码、资产、展示 schema 3 和相关文档完整集成。

本次自行修改限于文档合并、HUD 文档迁移和导航同步；不重新实现 HUD，不改变远端 C++ 或资产内容，不推送远端，不修改项目外 Skill。

## 2. 当前事实与依据

- 共同基线：`852c3cd`；本地合并前：`b067353`（包含 `00c6b21` 的文档迁移）；远端：`d844f0e`（底部 HUD）。
- 远端基于旧文档路径编写，三方合并冲突位于台账、网络展示专题、根 README 和已删除的旧索引。
- SAM 已获用户验收；远端旧状态不能覆盖本地明确验收。HUD 保留“工程验证完成，待用户实机复验”。
- 主 Skill：项目内 `combat-feature-development`，置信度 high；本次是已有功能的 Git/文档集成，无新游戏技能设计。

F0：GO。F1：APPROVED，保留两端提交历史并按当前目录职责合并文档；本记录在集成检查期间建立。

## 3. 行为与契约

- 远端 C++、Content、SourceArt 和 `.gitattributes` 与 `d844f0e` 保持一致。
- HUD 文档从远端平铺位置迁至 [10-12 底部 HUD](../10-Architecture/10-12-Bottom-HUD-Design.md)，一级标题、内部相对链接、台账、ADR、扩展指南与导航一起更新。
- 新增 HUD-001..003、ADR-047、拥有者快照与生命周期事实；HUD 的新增生命周期审计行移入当前 `10-12`，冻结的 `90-16` 保持原样。本地 AI-Native 流程和 SAM 验收保持。
- 保存远端的三 Target、57/57、资产 7/7、PIE/Dedicated 记录并注明来源；本次文档校验不替代运行时复测。

## 4. 验收标准（AC）

- [x] AC-01：两端提交保留，冲突标记和未合并 index 项清空。
- [x] AC-02：HUD 文档按目录编号、链接可达；本地 Skill、配置、工具和 SAM 验收保持。
- [x] AC-03：代码、资产和 LFS 实体与远端一致，没有文本改写二进制资产。
- [x] AC-04：本地校验通过，远端证据与本次实际执行的验证明确区分。

## 5. 测试矩阵与命令

| 检查 | 方式 | 结果 |
| --- | --- | --- |
| 文档与编号 | `python3 -B Tools/validate_docs.py` | 通过 |
| 校验工具回归 | `python3 -B -m unittest discover -s Tools/Tests -p 'test_validate_docs.py' -q` | 16/16 通过 |
| 冲突与空白 | Git 未合并项、冲突标记检索、暂存 diff 检查 | 通过 |
| 代码与资产保持 | 与 `origin/main` 的 Git blob 比较，新增/变更 LFS 文件的 SHA-256 和 size 检查 | 无差异；7 个资产通过 |

## 6. 交付证据

本机 UE 编译、Automation、蓝图/资产验证、PIE、Dedicated 未执行；本次仅按用户授权集成远端既有实现，运行时文件无手工改动。HUD 的运行时通过记录来自 `d844f0e` 的台账与文档，未冒充本机新结果。

回滚：合并未提交时可 `git merge --abort`；提交后如需撤回，应以合并提交为单位创建 revert，保留两端历史。

## 7. 自评与 Reflect

评分范围是本次合并与文档适配，不评价 HUD 功能是否已通过用户验收。

| 维度 | 权重 | 分数 | 证据与扣分原因 |
| --- | ---: | ---: | --- |
| 需求与 AC | 20% | 5 | 远端实现完整集成，目录与状态冲突已处理 |
| 架构与权限 | 20% | 4 | schema/ADR/权限说明一致；未在本轮开展 HUD 运行时专项审计 |
| 实现与数据 | 20% | 5 | 源码/资产 blob 与远端一致，LFS hash 与大小匹配 |
| 验证证据 | 20% | 4 | 文档、工具、Git 与 LFS 集成检查通过；UE Gate 使用原提交记录，未本机复跑 |
| 文档与可观测性 | 10% | 5 | `10-12`、导航、台账和证据来源完整 |
| 交付卫生 | 10% | 5 | 当前目录与项目内 Skill、配置保持；冲突标记已清理 |

总分 4.6/5，待用户复核；不改变 HUD 待实机验收状态。首次出现的是并行修改旧路径产生的合并冲突，先在当前任务记录处理方法；没有跨任务重复证据，不自动扩大 Skill 或流程规则。
